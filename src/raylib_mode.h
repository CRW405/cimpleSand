#ifndef RAYLIB_MODE_H
#define RAYLIB_MODE_H

#include <stdbool.h>

#define RAYLIB_DEFAULT_WIDTH 500
#define RAYLIB_DEFAULT_HEIGHT 500

void run_raylib_mode(bool player_enabled, bool history_enabled, int history_capacity);

#endif
